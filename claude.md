# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What This Is

A 24/7 real-time TV broadcast dashboard for **Indulge**, an Indian luxury concierge agency. It runs fullscreen on a 4K office TV (no cursor, no scroll). `/` is a landing page that asks which dashboard to view; `/concierge` opens the **Concierge** screen (three teams called "Queendoms" — *Anishqa*, *Sanika* and *Ananyshree*, in TV order — handling Freshdesk tickets) and `/onboarding` opens the **Revenue/Onboarding** screen (Zoho CRM leads and deals — one Onboarding roster across two agent columns; the Shop department was retired 2026-09-07, see `lib/onboardingAgents.ts`). Both screens stay mounted and can still be switched/rotated from either route. Data is pushed into Supabase by webhooks; the browser subscribes via Supabase Realtime, so the TV updates within seconds.

**Deeper references in this repo** (read these before broad changes):

- `docs/master.md` — complete reference: every screen/panel, design system, DB schema, API surface, rosters. Compiled 2026-06-11; supersedes `blueprint.md` / `design.md` where they disagree.
- `docs/dry-audit.md` — refactor audit (2026-06-11): known duplication, dead code, and contradictions, with an active phased cleanup plan. Check it before "fixing" something that looks redundant — it may already have a decided plan.

## Commands

```bash
npm run dev              # Next.js dev server
npm run build            # production build
npm run lint             # next lint
npm run import-tickets   # one-off CSV ticket import (tsx scripts/importTickets.ts)
npm run audit-freshdesk  # read-only Freshdesk ↔ DB ↔ dashboard diff (needs .env.local sourced; DASHBOARD_URL optional)
npm run reconcile-zoho -- --leads <ndjson…> --deals <ndjson> [--apply] [--delete-missing]   # Zoho COQL dumps → leads/deals (Zoho = truth)
```

There is no test framework configured.

Stack: Next.js App Router + React 18 + TypeScript, Tailwind CSS, Framer Motion, Supabase (`@supabase/supabase-js`).

## Critical Invariants

Violating any of these causes **silently wrong metrics on the TV**:

1. **IST timezone is mandatory.** All "today" / "this month" logic goes through `lib/istDate.ts` (`istToday()`, `getCurrentIstMonthUtcBounds()`, …). Never use `new Date().toISOString().slice(0, 10)` — that's UTC, not India time.
2. **VOID tickets (`spam`, `deleted`) are stripped before ALL math** — the first step of every aggregation. They never count toward Received, Resolved, Pending, or agent stats.
3. **TERMINAL statuses are `{resolved, closed}` only.** `spam`/`deleted` are void, not terminal. The authoritative sets (and the webhook's SLA policy sets) live in `lib/ticketStatus.ts` — never redefine them locally.
4. **Cohort math is anchored to `created_at`.** "Resolved This Month" = tickets *created* this IST month whose status is now terminal — not tickets resolved this month.
5. **`is_escalated` can only be set `true` by the escalation-only webhook path** in `app/api/webhooks/freshdesk/route.ts`. The full-upsert path must omit the field for active non-SLA-safe statuses (preserves the DB value).
6. **Freshdesk timestamps are naive IST strings.** Always pass them through `freshdeskTimestampToIsoUtcForDb()` (`lib/istDate.ts`) before storage, or Postgres will treat them as UTC and shift everything by +5:30.
7. **The browser Supabase client (anon key, `lib/supabase.ts`) is for Realtime ONLY.** All data fetching goes through Next.js API routes using `supabaseAdmin` (service role, `lib/supabaseAdmin.ts`). Both clients are module-level singletons — never call `createClient` inside a component or hook.
8. **Queendom/group name matching uses `.includes()`, not equality** (Freshdesk sends e.g. `"Team Ananyshree"`, `"Sanika's Queendom"`) — always via `normalizeQueendom()` in `lib/queendom.ts`.
9. **Postgres error `23505` (PK violation) on `leads.lead_id` or `deals.deal_id` is expected dedup behavior**, not an error — silently ignore it.
10. **Soft-delete only.** Tickets are never `DELETE`d; deletion sets `status = "deleted"` and the VOID filter hides the row.
11. **Both screens stay always-mounted — ON THE TV TREE ONLY.** Rotation in `DashboardController.tsx` only crossfades opacity/zIndex — never unmount a screen to "optimize". The mobile tree (`components/mobile/`, added 2026-08-20) deliberately does the opposite: only the active tab renders (battery), rotation is replaced by tabs, and the marquee ticker by static alert lines. `components/DashboardRoot.tsx` splits the two by media query — its MOBILE_QUERY must stay identical to the mobile CSS override in `app/globals.css` (which unlocks html/body scrolling and resets the TV's root font-size slope on phones).
12. **Viewing can be passcode-gated** via `DASHBOARD_PIN` (lib/viewerGate.ts) — OPT-IN: unset → open (the TV deployment needs no config), set → 30-day signed cookie per device. Distinct from SETTINGS_PIN. A Supabase Auth replacement was built and rolled back on 2026-09-07 (user decision: stay on PINs for now); the design notes live in memory if it is revisited.
13. **The queendom list is a registry, never a literal.** `QUEENDOM_IDS` / `queendomRecord()` in `lib/queendom.ts` drive every per-queendom shape (`Record<QueendomId, T>` in hooks, routes, aggregation, mobile). Never write `{ ananyshree, anishqa, … }` by hand or branch on a specific id in UI/data code. Adding a queendom (done for Sanika, 2026-09-04) = extend `QueendomId` + the registry + a `ROSTER_*` fallback + a Special Dates section + a migration that widens the `agents` CHECK, the renewals/members label trigger and Postgres `live_queendom_id()` (the SQL twin of `normalizeQueendom()` that every `insights_*` function scopes through).

14. **Zoho lead statuses go through `normalizeLeadStatus()` (`lib/leadStatus.ts`) — never compare `latest_status` literally.** Zoho renamed its `Lead_Status` picklist around 2026-09-03 (New → RNR / Conversing / Nurturing / Cold / Payment Link / Win / Lost / Junk / Not Qualified) and a picklist rename does NOT fire the field-update webhook, so the DB still carries `Touched` / `In Discussion` / `Qualified` on rows last modified before the rename (verified 58/58 `Touched` = Zoho `RNR` on 2026-09-07). The legacy map lives in that one file; the health-bar palette (`STATUS_COLORS`) and `AgentLeadStatusBreakdown` are keyed by `ZOHO_LEAD_STATUSES`, so adding a status = extend that array + one swatch.
15. **The Onboarding roster is `ONBOARDING_AGENT_CARDS` in `lib/onboardingAgents.ts`** (Samson, Kaniisha | Nandini, Kabeer, Surbhi as of 2026-09-07 — matched to Zoho owner full names via `zohoName` / first-name fallback). Rows owned by anyone else (chiefly the automation owner "Admin @ Indulge", ~160 leads/month) count toward the month tiles and trendline but never toward a card. **Metrics are scoped to `METRIC_BUSINESS_VERTICAL` = Indulge Global** (`lib/onboardingTypes.ts`; cards, pipeline, tiles — the by-vertical trendline keeps all four lines; `deals` has no vertical column). Portraits: `onboarding-agents-images/<id>.webp` + the map in `components/onboarding/utils.ts`; seats without a file show a blank black frame (user decision 2026-09-07).

## Architecture

### Data flow (push-based, no client-side CRM calls)

```
Freshdesk ticket event → POST /api/webhooks/freshdesk → upsert `tickets`
Zoho lead event        → POST /api/webhooks/zoho-leads → upsert `leads`
Zoho deal event        → POST /api/webhooks/zoho-deals → insert `deals`
Google Sheet sync      → `jokers` table (external)
        ↓ Supabase Realtime push
hooks/useDashboardData.ts        → concierge screen state (tickets/clients/jokers/renewals channels)
hooks/useOnboardingPanelData.ts  → revenue screen state (deals/leads channels, debounced /api/onboarding refetch)
        ↓
app/page.tsx (landing: components/landing/DashboardPicker) → /concierge | /onboarding (components/GatedPage + DashboardRoot)
components/Dashboard.tsx → DashboardController.tsx (rotation) → concierge/ConciergeScreen / OnboardingLayout / HomePanel
ConciergeScreen = ScoreboardStrip (names · membership pills · ticket metrics per queendom — Spoiled tile hidden behind `SHOW_SPOILED_TILE`, code kept) + one QueendomColumn per QUEENDOM_IDS (RenewalsPanel → AgentLeaderboard + SpecialDates 2×3 → ResolveStopwatch | UpcomingRenewals)
```

- **Design handoff for the three-column concierge screen** lives outside the repo at `../design_handoff_three_queendoms/README.md` (option 1c, authored at the TV's 6144 × 3456 stage). New stage-pinned sizes go through `lib/tvScale.ts` (`fw(px)` / `fh(px)` → `clamp()` that lands on the exact pixel on the TV); existing tokens (`--pad-*`, `.label-field`, the leaderboard clamps) already hit the handoff values — keep using them.
- **The overdue ticker shows an overdue-age badge** (`OverdueTicker.tsx`) — always red because overdue is an error; standard red under 24 h, neon red from 24 h (user decision 2026-09-04). It counts from `OverdueTicketItem.overdueSince` = `tickets.due_by` (the SLA deadline whose breach flipped `is_escalated`), NOT from creation (user decision 2026-09-05). Computed at render; refreshes with the 5-min poll / realtime refetch, never on a timer. `/api/tickets/overdue` and `/api/tickets/overdue-list` return **open AND escalated** tickets **of the live Queendoms** (`normalizeQueendom()`; Retail / Indulge Shop / Jokers / Finance escalations are out of scope) — the leaderboard's Overdue definition — excluding terminal/void statuses case-insensitively (spam rows with a stale `is_escalated = true` were leaking into the marquee until 2026-09-05), so the ticker and the Genies' overdue counts agree.

- **Ticket aggregation happens client-side**: `/api/tickets/rows` returns minimal rows; `lib/ticketAggregation.ts` computes all queendom/agent metrics in the browser and re-derives them on each Realtime patch. The webhook route in `app/api/webhooks/freshdesk/route.ts` is a three-path dispatcher (deletion / escalation-only / full upsert) — read its header comment before touching it.
- **Screen rotation config lives in `lib/dashboardScreens.ts`**: order, per-screen durations (concierge 60s, onboarding 10s, home 30s), and the `NEXT_PUBLIC_HOME_PANEL_ENABLED` gate for the WIP Home screen (`components/HomePanel.tsx`).
- **Keyboard/TV-remote controls** (`hooks/useKeyboardControls.ts`): `P`/`Space`/`Enter` = freeze toggle, arrow keys = manual screen switch, `Escape`/`Backspace` = back to the landing page (also the MENU segment of the top-right pill). On the landing page arrows move focus between the two plinths and Enter opens the focused one.
- **`components/_unmounted/`** holds built-but-not-mounted widgets (finance outlays, bar/velocity charts). Don't mount them without wrapping in `ErrorBoundary`; don't delete them.
- The legacy `/api/tickets` (server-side aggregation) and `/api/agents` routes were deleted 2026-06-11 (dry-audit G1) — `/api/tickets/rows` + client-side aggregation is the only ticket path.

### Database (Supabase, `supabase/migrations/`)

Core tables: `tickets` (Freshdesk, PK `ticket_id`), `leads` (Zoho, PK `lead_id`), `deals` (Zoho, PK `deal_id`), `onboarding_conversion_ledger` (sales ledger UI), `jokers`, `clients`, `renewals`, `members`, `agents`, `finance_outlays`. All have Realtime enabled and the same RLS pattern (anon SELECT, authenticated ALL; service role bypasses). Canonical names after renames: `leads` (not `onboarding_lead_touches`), `deals` (not `onboarding_deals`). Full column-level schema is in `docs/master.md` §8.

## Environment Variables

| Variable | Purpose |
|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | both Supabase clients |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | browser client (Realtime only) |
| `SUPABASE_SERVICE_ROLE_KEY` | `supabaseAdmin` — all API routes return 503 if missing |
| `WEBHOOK_SECRET` | webhook auth (`x-webhook-secret` header); **fail-closed in production** if unset, fail-open in dev |
| `NEXT_PUBLIC_HOME_PANEL_ENABLED` | set `true` to preview the WIP Home screen in rotation |
| `SETTINGS_PIN` | shared 4-digit PIN for `/settings` (6482 since 2026-09-07, set locally and on Vercel); **fail-closed in production** if unset, fail-open in dev (same policy as `WEBHOOK_SECRET`) |

### Settings (`/settings`, added 2026-08-11; restructured 2026-09-07)

A PIN-locked admin surface so non-technical staff can change things that used to require a code edit. Reached from the Settings plate on the landing page (`components/landing/DashboardPicker.tsx` — the gear appears ONLY there, never on the dashboards). Flow: 4-digit PIN (`components/settings/SettingsShell.tsx`, shared by every settings screen, auto-submits on the fourth digit) → **domain picker** (`app/settings/page.tsx`: Concierge live, Onboarding shown disabled/"coming soon") → `/settings/concierge?q=<queendom>&s=agents|renewals|members` (`app/settings/concierge/page.tsx`): a queendom switcher in TV order, then a section switcher. Per-queendom editors: `components/settings/QueendomAgents.tsx` (add · **rename** · hide/show · move · remove, the Joker, and the "filing tickets but not on the roster" notice — plus an info notice when a queendom has zero rows and the TV is on the code fallback) and `components/settings/QueendomClientRows.tsx` (renewals / members: add · **edit name+date** · remove; `lib/settingsClientRows.ts` gained PATCH). Notes that matter:

- **The roster now lives in the `agents` table**, not `lib/agentRoster.ts`. `GET /api/roster` serves it; `useDashboardData` threads it into `mergeAndRankAgents(rows, roster)`. The hardcoded arrays remain as `FALLBACK_ROSTER` and are still the default parameter — an empty, missing, or unreachable `agents` table leaves the TV exactly as it was, so **do not delete them**.
- **Anything this app writes to `renewals` / `members` must use `QUEENDOM_LABEL`** (`lib/queendom.ts`) — `"Ananyshree Queendom"` / `"Anishqa Queendom"` / `"Sanika Queendom"`. Those two tables were normalised to that form on 2026-08-11 (from `"ananyshree"` and `"Ananyshree's Queendom"` respectively) and a BEFORE-trigger keeps them there. `tickets` / `jokers` / `clients` were deliberately left on their own spellings — reads still go through `normalizeQueendom()`.
- **`renewals.created_at` / `members.created_at` are the business date, not the insert time** (renewal date / assignment date). The panel month-gates on them, so the Settings form writes a chosen date through `timestampStringToIsoUtcForDb()` — IST midnight.
- `/api/settings/*` routes verify the session cookie themselves via `withSettingsGuard`; the UI's PIN gate is convenience only.

## Known Sharp Edges

- **Overdue and Incomplete carry forward; every other metric (incl. Pending) is month-gated** (dry-audit **D2**, revised 2026-07-02): an escalated or incomplete ticket from an earlier month keeps counting until cleared, so the leaderboard's overdue/incomplete scores never reset at month rollover — Overdue can therefore exceed the month-gated Pending. All other metrics (Received / Resolved / Today / Monthly / Pending / Joker) are gated by explicit `toISTDay`/`toISTMonth` checks in `lib/ticketAggregation.ts`. The rows route fetches "current IST month ∪ open backlog" and `pruneTicketRowsForDashboardState` keeps the same set. Any NEW metric must choose its gate explicitly — the input rows are no longer month-only.
- Both data hooks now share `hooks/useRealtimeChannel.ts` (5-min poll + `CHANNEL_ERROR`/`TIMED_OUT` → refetch + 3s resubscribe; dry-audit **C2**). Channel names (`dashboard-*`, `deals-live`, `leads-touches-live`) are contractual — never rename them, and always clean up via `removeChannel` (the shared hook does both).
- Hidden screen layers stay mounted but pause their own clocks via `useScreenActive()` (rAF ledger scroll, HomePanel clocks) — resume is seamless because state lives in refs (dry-audit **H3/H4**).
- **Never bulk-import ticket CSVs through the Supabase table editor** — Freshdesk exports carry naive IST wall-clock timestamps and the table editor stores them verbatim as UTC, shifting every instant +5:30 into the future (froze the ResolveStopwatch at 00:00 on 2026-07-03, and skewed every IST-gated metric). The only sanctioned path is `npm run import-tickets -- <csv>` (`scripts/importTickets.ts`), which routes all timestamps through `lib/istDate.ts` — the same conversion service the Freshdesk webhook uses — and aborts if any converted timestamp lands in the future. It accepts both Freshdesk-style and DB-style CSV headers. Note a wipe-and-reimport resets webhook-maintained columns not present in exports (`is_incomplete`, `tags`).
- **Freshdesk ↔ DB audit 2026-09-05** (full diff of every ticket updated this month + the open backlog, then dashboard math recomputed from Freshdesk): the dashboard renders the DB exactly (`/api/tickets/rows` = DB, aggregation = DB), so any wrong number on the TV is DB drift, not UI. Drift found and healed via `?hours=168`: 20 tickets never arrived by webhook (bursts of tickets created minutes apart), 29 stale statuses, 4 stale assignees, 60 rows with arrival-time `created_at`. Root cause of the persistence: the GitHub cron runs 5–6.5 h apart while the lookback was 4 h — now `?hours=48` (workflow) / `?hours=168` (daily Vercel backstop). Re-audit after the heal: 0 status / agent / created_at / resolved_at diffs, all queendom metrics identical. **Open question for ops — the Overdue definition**: `is_escalated` is set only by the Freshdesk automation that POSTs `is_escalated: true`; for 23 of the 25 open escalated tickets Freshdesk's own `is_escalated` AND `fr_escalated` flags are false, so that automation's condition is not Freshdesk's SLA breach. Check the rule's trigger before trusting Overdue as "SLA breached". Do NOT switch to `due_by < now` — 515 of 674 open tickets are past `due_by` because paused statuses (Nudge Client / Ongoing Delivery / Invoice Due) keep a stale deadline.
- **Freshdesk automation map (read via API 2026-09-05 — the dashboard's inputs live HERE, not in this repo).** Rules that POST to `/api/webhooks/freshdesk`: *Push New Tickets to Dashboard* (creation), *Live Dashboard Updation Rule* (any update, `webhook_type: resolution`), *Ticket deletion update*, *Dashboard Updation - Ticket Marked as Spam*, *Push Overdue to Dashboard* (system event "Resolution due" = SLA breach → `is_escalated: true`), *Clear SLA breach flag when deadline extended* (agent edits due_by → `is_escalated: false`). Defects found on that read — (1) and (2) FIXED by the user in Freshdesk admin on 2026-09-06 and re-verified via API: (1) every rule except *Push Overdue* was filtered to the Ananyshree + Anishqa group ids only, so Sanika's group (1070000391947) got no webhooks — now all five rules include her; (2) *Marked as Spam* was wired to the **SLA-breach event** ("marked overdue for resolution"), so every breaching Queendom ticket was written to the DB as `spam` and vanished from the TV until the cron restored it — now performer Agent + event `marked_spam`. (5) FOUND 2026-09-07: **the creation rule is not firing at all** — 27/27 rows that reached the DB after the 03:43Z cron carried arrival-time `created_at` (microsecond fingerprint = came via the UPDATE webhook, which has no `ticket_created_at`), and 23 brand-new tickets were absent until healed. Most likely cause: Freshdesk *Ticket Creation* automations execute **only the first matching rule** by default, and *New Ticket Creation Update On App* (position 1, same group condition) sits above *Push New Tickets to Dashboard* (position 2). Fix in Freshdesk admin: Automations → Ticket Creation → enable "Execute all matching rules" (do NOT just reorder — that would silence the app instead). Cheap belt-and-braces: add `"ticket_created_at": "{{ticket.created_at}}"` to the *Live Dashboard Updation Rule* payload so update-first arrivals carry the real creation time. Still open: (3) *Push Overdue* has NO group filter (dead groups' breaches arrive; the overdue routes filter them out) and only excludes status 4, not Closed; (4) the live Queendom groups fall under the **Default SLA policy** (resolve within 8 business hours 09:00–20:00 Mon–Sat, all priorities) whose `escalation` block is empty — so Freshdesk's own `is_escalated` never flips and the DB flag has no truth source (the cron can never set it true). Fix these in Freshdesk admin; the code cannot compensate for (2).
- **Sanika's Queendom has no Joker on file** (2026-09-04) — her Spoiled value reads 0 until a `role = 'joker'` row is added in /settings (the tile itself is hidden for every queendom via `SHOW_SPOILED_TILE` in `ScoreboardStrip.tsx` since 2026-09-05 — flip it, don't re-add markup). Her roster was seeded from the names filing under her Freshdesk group in September 2026 (Kshathriya C C A, Shanaya Javeri, Depender Kaur); Sanika Ahire's own row moved to `sanika` and stays hidden like the other queens. `lib/specialDates.ts` has no Sanika dates yet.
- **The Joker roster was stale and silently zeroing a whole panel** (found 2026-08-11): `JOKER_ROSTER` named `"Shruti Sharma"` for anishqa, who has zero rows in `jokers` — the 694 anishqa rows all belong to `"Anil Talluri"`, so every anishqa Joker metric read 0. `/api/jokers` now resolves joker names from the `agents` table (`role = 'joker'`). Separately, `jokers` has had **no row newer than 2026-03-31** and the route is IST-month-gated, so both panels read 0 regardless — that upstream sync looks dead and is worth checking before trusting the Joker panel.
- **`jokers.queendom_name` is misspelled both ways** — `"ananyashree"` (extra `a`, 770 rows) and `"anisqa"` (missing `h`, 694 rows). Neither resolves through `normalizeQueendom()`. Harmless today only because `/api/jokers` keys off `joker_name`; any new code that trusts that column will silently drop every row. `onboarding_conversion_ledger.queendom_name` is empty string in all 109 rows for the same class of reason.
- **Zoho automation map (read via API 2026-09-07).** One webhook per module, both hitting this app with `?secret=` in the URL (dry-audit E5 still applies): *To Dashboard* → `/api/webhooks/zoho-leads`, fired by *New Leads Score push to Dashboard* (Leads · create) and *Update Leads Data to Dashboard* (Leads · field update of `Lead_Status` OR `Owner` only); *send_update_to_dashboard* → `/api/webhooks/zoho-deals`, fired by *Update Closures to Dashboard* (Deals · create — every deal is created at `Closed Won`, so deals = closures). The leads webhook had a trickle of `bad_request` failures on the create rule (leads born without owner/status) — the route now defaults those to `Unassigned` / `New` instead of 400. Zoho's users API is the roster truth: Amit + Meghana disabled 2026-08-17, the Retail/Shop users deleted 2026-06-16, Nandini/Kabeer/Surbhi added 2026-08-17. **Zoho is the source of truth for `leads` / `deals`** (user decision 2026-09-07): `npm run reconcile-zoho -- --leads <ndjson…> --deals <ndjson> [--apply] [--delete-missing]` (`scripts/reconcileZoho.ts`) diffs COQL dumps against the DB and upserts owner / status / name / vertical / timestamps; it resolves owners by Zoho user id (COQL returns last names only), so extend its `OWNER_NAMES` when a user joins. No Zoho API credentials live in this repo — dumps come from the claude.ai Zoho MCP. **The 2026-09-07 dry run found 5,154 of 5,816 `leads.created_at` values +5:30 ahead of Zoho (IST digits stored as UTC — the invariant-6 failure class, via the webhook payload; the conversion code itself is correct for every ISO variant tested), 1,242 stale statuses (mostly the picklist rename), 646 stale owners, 2,586 leads missing entirely, and 39 `deals` rows Zoho no longer has (33 of them pre-webhook `zcrm_`-prefixed March/April imports).** The leads webhook now refuses future instants (`guardAgainstIstDigitsAsUtc`: > 5 min ahead of arrival → shift back 5:30, else arrival time) and logs the raw `created_at` / `modified_at` strings per request so the wire format is visible in Vercel logs.
- `POST /api/webhooks/zoho-calls` is not implemented (README scaffold only).
- `onboarding_conversion_ledger` is orphaned in the DB (nothing reads/writes it); drop/archive decision pending (dry-audit **G4**).
