# Assets

Supplemental diagrams for [`../CODEBASE_KNOWLEDGE.md`](../CODEBASE_KNOWLEDGE.md). Each SVG is the validated render of the corresponding Mermaid block in the master document (all three validated `valid: true` via the Mermaid renderer).

| File | Diagram | Section in master doc |
|---|---|---|
| `architecture-flowchart.svg` | Push-based data flow: webhooks → Supabase → Realtime → browser | §4.1 |
| `freshdesk-ticket-sequence.svg` | End-to-end Freshdesk ticket event sequence | §4.2 |
| `database-er-diagram.svg` | Database tables and logical relationships | §6 |

> The canonical source for each diagram is the Mermaid code block inside `../CODEBASE_KNOWLEDGE.md`; these SVGs are pre-rendered copies for viewers that don't render Mermaid.
